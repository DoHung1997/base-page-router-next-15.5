import React from 'react';

type PropsType = {}

const AuthContainer: React.FC<PropsType> = (props) => {
    return (
        <div>

        </div>
    );
};

export default AuthContainer;