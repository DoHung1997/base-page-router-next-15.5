import React from 'react';

type PropsType = {}

const RegisterForm: React.FC<PropsType> = (props) => {
    return (
        <div>
            Register form
        </div>
    );
};

export default RegisterForm;